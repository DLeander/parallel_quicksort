# A parallel quicksort algorithm.
A parallelized version of the standard c quicksort. test.c is merely included to make sure that the algorithm is functioning correctly. The algorithm may only be used with the number of threads being a power of 2.

## Usage
```c
#include "pquicksort.h"
```
